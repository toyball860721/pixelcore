apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
- ../../apps/pixelcore-api

namespace: pixelcore-dev

namePrefix: dev-

commonLabels:
  environment: dev

replicas:
- name: api
  count: 1

images:
- name: pixelcore/api
  newTag: dev-latest
