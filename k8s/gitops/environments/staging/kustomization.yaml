apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
- ../../apps/pixelcore-api

namespace: pixelcore-staging

namePrefix: staging-

commonLabels:
  environment: staging

replicas:
- name: api
  count: 2

images:
- name: pixelcore/api
  newTag: staging-v1.0.0
